// create component called 'App'; use React.createClass
App = React.createClass( {
	// add ReactMeteorData to use data for view
	mixins : [ReactMeteorData],
	// get data from database(collection) and store it in variable
	getMeteorData : function() {

	},

	render : function() {
		return ();

	}

} );